import java.awt.*;

public class Main {
    /**
     * Once called, the game of chess will begin, can be easily integrated to the state machine
     * where the state machine can call the method to play the game.
     */
    public static void play() {
    }

    /**
     * Once called, the game of chess will pause, can be easily integrated to the state machine
     * where the state machine can call the method to pause the game.
     */
    public static void pause() {
    }
}
